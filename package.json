{
  "name": "integration",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:once": "npm run test:once --workspaces --if-present",
    "fix": "gts fix",
    "lint": "gts lint"
  },
  "devDependencies": {
    "@types/node": "20.x.x",
    "@vitest/coverage-v8": "0.34.4",
    "gts": "5.0.1",
    "tsup": "7.2.0",
    "typescript": "5.2.2",
    "vitest": "0.34.4"
  },
  "workspaces": ["packages/*"]
}
