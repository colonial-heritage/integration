{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "baseUrl": ".", // This must be specified if "paths" is
    "paths": {
      // This is necessary for the tests to find local packages and for compilation to work in any order of packages
      "@colonial-collections/*": ["./packages/*/src"]
    }
  }
}
